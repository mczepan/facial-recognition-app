import React from 'react';
import './Navigation.css';

const Navigation = () => {
	return (
		<nav>
			{/*<nav style={{ display: 'flex', justifyContent: 'flex-end' }}>
		       <nav style={{ textAlign: 'right' }}> */}
			<p className="p-nav">Sign out</p>
		</nav>
	);
};

export default Navigation;
